# storage

## localStorage1.js

```
$ deno run -A localStorage1.js 
You have started the application 0 times previously.
$ deno run -A localStorage1.js 
You have started the application 1 times previously.
$ deno run -A localStorage1.js 
You have started the application 2 times previously.
```
